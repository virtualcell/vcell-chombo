# This repo was split out of virtualcell/vcell-solvers, where a mix of CRLF and
# LF line endings is already committed (VCellChombo came from a Windows/NetBeans
# checkout).  Disable git's end-of-line conversion so the files here stay
# byte-identical to their originals and diffs against the old tree stay readable.
* -text
